<div class="d-flex justify-content-center">
  <div class="col-4">
    <%= simple_form_for [@list, @review], :class => "form-group" do |f| %>
      <%= f.input :comment, :class => "form-control"%>
      <%= f.input :rating, :class => "form-control"%>
      <%= f.submit "Submit review", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
